# 🚀 Инструкция по деплою на Beget

## ✅ Шаг 1: Подготовка (выполнено)

- ✅ Production build собран (`npm run build`)
- ✅ Создан файл `.htaccess` для работы SPA
- ✅ Все статические файлы готовы в папке `dist/`

## 📁 Что загружать на хостинг

Все файлы из папки **`dist/`**:

```
dist/
├── .htaccess          ← Важно! Для работы роутинга
├── index.html         ← Главная страница
├── logo.svg           ← Логотип
├── robots.txt         ← SEO
└── assets/            ← JS и CSS
    ├── index-ByP_UAw-.css
    ├── index-DQt857Mk.js
    └── react-vendor-D7amQhf0.js
```

## 🌐 Шаг 2: Вход в панель Beget

1. Перейдите на https://beget.com/
2. Войдите в личный кабинет
3. Перейдите в раздел **"Файловый менеджер"** или используйте **FTP**

## 📂 Шаг 3: Размещение файлов

### Вариант А: Через Файловый менеджер Beget (рекомендуется)

1. **Откройте Файловый менеджер** в панели Beget
2. **Перейдите в корневую папку сайта**:
   - Обычно это: `yourdomain.ru/public_html/`
   - Или: `yourdomain.ru/www/`
3. **Удалите старые файлы** (если есть):
   - Оставьте только `.htpasswd`, `.well-known` и другие служебные папки
   - Удалите старые `index.html`, папки `css/`, `js/` и т.д.
4. **Загрузите ВСЕ файлы из папки `dist/`**:
   - Выберите "Загрузить файлы"
   - Выберите все файлы из папки `dist/` на вашем компьютере
   - Дождитесь окончания загрузки
5. **Проверьте структуру**:
   ```
   yourdomain.ru/public_html/
   ├── .htaccess
   ├── index.html
   ├── logo.svg
   ├── robots.txt
   └── assets/
       ├── index-ByP_UAw-.css
       ├── index-DQt857Mk.js
       └── react-vendor-D7amQhf0.js
   ```

### Вариант Б: Через FTP (FileZilla, Cyberduck и т.д.)

1. **Получите FTP данные** из панели Beget:
   - Раздел "FTP/SSH"
   - Хост: обычно `ftp.yourdomain.ru`
   - Логин: ваш логин
   - Пароль: ваш пароль
   - Порт: 21

2. **Подключитесь через FTP-клиент**:
   - FileZilla: https://filezilla-project.org/
   - Или Cyberduck: https://cyberduck.io/

3. **Загрузите файлы**:
   - Локальная папка (слева): `/Users/eldar/эльдар.маркетинг/dist/`
   - Удаленная папка (справа): `/yourdomain.ru/public_html/`
   - Перетащите все файлы из `dist/` в `public_html/`

## 🔍 Шаг 4: Проверка

1. **Откройте браузер** и перейдите на ваш домен
2. **Проверьте основные страницы**:
   - Главная: `https://yourdomain.ru/`
   - Услуги: `https://yourdomain.ru/#` (кликните в меню)
   - Кейсы: `https://yourdomain.ru/#` (кликните в меню)
3. **Проверьте прямые ссылки** (важно для SEO):
   - Обновите страницу (F5) - должна открываться без ошибки 404
4. **Проверьте мобильную версию**

## ⚙️ Шаг 5: Настройка домена (если нужно)

### Если используете поддомен beget

Сайт будет доступен по адресу:
- `http://username.beget.tech/`

### Если у вас свой домен

1. **Привяжите домен** в разделе "Домены"
2. **Настройте DNS**:
   - A-запись: укажите IP-адрес из панели Beget
   - Или используйте NS-серверы Beget
3. **Дождитесь обновления DNS** (до 24 часов, обычно 1-2 часа)

## 🔒 Шаг 6: SSL сертификат (HTTPS)

1. Перейдите в раздел **"SSL-сертификаты"**
2. Выберите **"Let's Encrypt"** (бесплатный)
3. Нажмите **"Получить сертификат"**
4. Через 5-10 минут сайт будет доступен по HTTPS

### Принудительное перенаправление на HTTPS

Добавьте в начало `.htaccess` (если нужно):

```apache
# Перенаправление на HTTPS
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
```

## 🔄 Обновление сайта (после изменений)

Когда вы внесли изменения в код:

1. **Пересоберите проект**:
   ```bash
   npm run build
   ```

2. **Загрузите новые файлы**:
   - Удалите старую папку `assets/` на хостинге
   - Загрузите новые файлы из `dist/`
   - Замените `index.html`

3. **Очистите кэш**:
   - В браузере: Ctrl+Shift+R (hard reload)
   - В панели Beget: если есть кэширование

## 🐛 Устранение проблем

### Проблема: 404 ошибка при обновлении страницы

**Решение**: проверьте, что файл `.htaccess` загружен на хостинг

### Проблема: Белый экран

**Решение**: 
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в консоли
3. Убедитесь, что все файлы из `assets/` загружены

### Проблема: Логотип не отображается

**Решение**: убедитесь, что `logo.svg` находится в корневой папке сайта

### Проблема: Стили не применяются

**Решение**: 
1. Очистите кэш браузера (Ctrl+Shift+Del)
2. Проверьте, что файл CSS загружен в папку `assets/`
3. Проверьте пути в `index.html`

### Проблема: Медленная загрузка

**Решение**:
1. Убедитесь, что gzip сжатие работает (проверьте `.htaccess`)
2. Используйте CDN (опционально)
3. Проверьте размер изображений

## 📊 Рекомендации после деплоя

### 1. SEO оптимизация

- ✅ `robots.txt` уже загружен
- 📝 Создайте `sitemap.xml` (опционально)
- 🔍 Добавьте meta-теги для соцсетей (Open Graph)

### 2. Аналитика

Добавьте в `index.html` перед `</head>`:

```html
<!-- Яндекс.Метрика -->
<script type="text/javascript">
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();
   for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
   k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

   ym(XXXXXX, "init", {
        clickmap:true,
        trackLinks:true,
        accurateTrackBounce:true
   });
</script>
```

### 3. Мониторинг

- Настройте Google Search Console
- Настройте Яндекс.Вебмастер
- Проверьте скорость на PageSpeed Insights

## 📁 Структура после деплоя

```
yourdomain.ru/public_html/
├── .htaccess              ← Роутинг + кэширование + безопасность
├── index.html             ← Главная страница
├── logo.svg               ← Логотип сайта
├── robots.txt             ← SEO настройки
└── assets/                ← Оптимизированные JS и CSS
    ├── index-*.css        ← Стили + анимации
    ├── index-*.js         ← Основной код
    └── react-vendor-*.js  ← React библиотека
```

## ✅ Чеклист перед запуском

- [ ] Все файлы из `dist/` загружены
- [ ] `.htaccess` находится в корне
- [ ] Сайт открывается по домену
- [ ] Роутинг работает (переходы между страницами)
- [ ] Обновление страницы не дает 404
- [ ] Логотип отображается
- [ ] Анимации работают
- [ ] Мобильная версия выглядит корректно
- [ ] SSL сертификат установлен (HTTPS)
- [ ] Контакты работают (ссылки на Telegram, телефон, email)

---

## 🎉 Готово!

Ваш сайт с профессиональными анимациями теперь живет на Beget!

**Сайт будет доступен по адресу**: `https://yourdomain.ru`

### Полезные ссылки:

- 📚 База знаний Beget: https://beget.com/ru/kb
- 💬 Поддержка Beget: support@beget.ru
- 🔧 Панель управления: https://cp.beget.com/

---

**Время деплоя**: ~10-15 минут  
**Создано**: 2025  
**Проект**: eldar.marketing

