# 🎨 Интерактивные элементы сайта eldar.marketing

Документация по добавленным интерактивным анимациям и эффектам.

## 📋 Реализованные функции

### 1. **Scroll Progress Bar** (Индикатор прогресса чтения)

**Расположение:** Тонкая линия в самом верху страницы

**Функционал:**
- Показывает прогресс прочтения страницы (0-100%)
- Плавное движение при скролле
- Градиентная заливка (primary → accent)
- Не мешает навигации (pointer-events: none)

**Компоненты:**
- `src/components/ScrollProgressBar.tsx`
- `src/components/ScrollProgressBar.css`

**Характеристики:**
- Высота: 2px
- Цвет: Градиент от `--color-primary` до `--color-accent`
- z-index: 10000 (поверх всего контента)
- Accessibility: Убирается при `prefers-reduced-motion`

---

### 2. **Animated Counter** (Анимированный счетчик)

**Где используется:** Секция "Кто мы" → Статистика

**Функционал:**
- Цифры анимированно считаются от 0 до целевого значения
- Запускается при появлении элемента в viewport
- Плавная анимация с easing (easeOutCubic)
- Срабатывает только один раз

**Компонент:**
- `src/components/AnimatedCounter.tsx`

**API:**
```tsx
<AnimatedCounter 
  end={50}              // Конечное значение
  duration={2500}       // Длительность анимации (мс)
  suffix="+"            // Суффикс после числа
  className="stat-value" // CSS класс
/>
```

**Примеры на сайте:**
- `10+` лет опыта
- `60+ млн ₽` бюджета
- `50+` проектов
- `7+` лет с федеральными брендами

**Threshold:** Элемент становится видимым при 30% появления в viewport

---

### 3. **Scroll Reveal** (Появление при скролле)

**Где используется:** Практически все секции главной страницы

**Функционал:**
- Элементы плавно появляются при прокрутке до них
- 4 типа анимаций: fadeInUp, fadeInLeft, fadeInRight, scaleIn
- Настраиваемая задержка для каскадного эффекта
- Intersection Observer API для производительности

**Компоненты:**
- `src/components/ScrollReveal.tsx` - React компонент
- `src/components/ScrollReveal.css` - Стили анимаций
- `src/hooks/useScrollReveal.ts` - Хук для функционала

**API:**
```tsx
<ScrollReveal 
  animation="fadeInUp"  // Тип анимации
  delay={100}           // Задержка (мс)
  className=""          // Дополнительный класс
>
  <div>Контент</div>
</ScrollReveal>
```

**Типы анимаций:**

1. **fadeInUp** (по умолчанию)
   - Появление снизу вверх
   - Используется для: карточек услуг, кейсов

2. **fadeInLeft**
   - Появление слева направо
   - Используется для: шагов подхода

3. **fadeInRight**
   - Появление справа налево
   - Зарезервировано для будущих элементов

4. **scaleIn**
   - Увеличение от 90% до 100%
   - Используется для: промо-блоков (Planeta, E-commerce)

**Примеры использования на главной:**

```tsx
// Заголовки секций
<ScrollReveal>
  <h2>Услуги</h2>
</ScrollReveal>

// Карточки с каскадом
{SERVICES_PREVIEW.map((service, idx) => (
  <ScrollReveal key={service.id} delay={idx * 100}>
    <div className="card">...</div>
  </ScrollReveal>
))}

// Промо-блоки с масштабированием
<ScrollReveal animation="scaleIn">
  <div className="planeta-promo-content">...</div>
</ScrollReveal>
```

---

## 🎯 Настройки и параметры

### Timing Functions (animations.css)
```css
--ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);
--ease-enter: cubic-bezier(0, 0, 0.2, 1);
--ease-exit: cubic-bezier(0.4, 0, 1, 1);
```

### Duration
```css
--duration-instant: 150ms;
--duration-fast: 250ms;
--duration-normal: 400ms;
--duration-slow: 600ms;
--duration-very-slow: 900ms;
```

### Delays (каскадные анимации)
```css
--delay-step: 80ms;
```

### Intersection Observer настройки

**ScrollReveal:**
- threshold: 0.1 (10% элемента в viewport)
- rootMargin: '0px 0px -100px 0px' (срабатывает раньше)
- triggerOnce: true (один раз)

**AnimatedCounter:**
- threshold: 0.3 (30% элемента)
- rootMargin: '0px'

---

## ♿ Accessibility

Все анимации учитывают пользовательские настройки:

```css
@media (prefers-reduced-motion: reduce) {
  .scroll-reveal-wrapper {
    opacity: 1 !important;
    transform: none !important;
    transition: none !important;
  }
}
```

При включенной настройке "Reduce motion":
- Анимации отключаются
- Элементы сразу видимы
- Transitions убираются

---

## 🚀 Производительность

### Оптимизации:

1. **Intersection Observer** вместо scroll events
   - Нативное API браузера
   - Автоматическая оптимизация
   - Нет постоянного слушания scroll

2. **will-change** для GPU acceleration
   ```css
   will-change: opacity, transform;
   ```

3. **requestAnimationFrame** для счетчика
   - Плавная анимация 60 FPS
   - Синхронизация с браузером

4. **passive listeners** для scroll
   ```tsx
   window.addEventListener('scroll', handler, { passive: true })
   ```

5. **CSS transforms** вместо position/margin
   - Не вызывают reflow
   - Работают на GPU layer

---

## 📱 Адаптивность

Все анимации работают на всех устройствах:

- **Desktop**: Полная функциональность
- **Tablet**: Полная функциональность
- **Mobile**: Полная функциональность с оптимизацией
- **Low-end devices**: Graceful degradation через prefers-reduced-motion

---

## 🔧 Расширение функционала

### Добавление новой анимации в ScrollReveal:

1. Добавить keyframe в `ScrollReveal.css`:
```css
.scroll-reveal-wrapper.myAnimation {
  transform: rotate(0deg);
}

.scroll-reveal-wrapper.myAnimation.is-revealed {
  opacity: 1;
  transform: rotate(360deg);
}
```

2. Добавить тип в TypeScript:
```tsx
animation?: 'fadeInUp' | 'fadeInLeft' | 'fadeInRight' | 'scaleIn' | 'myAnimation'
```

3. Использовать:
```tsx
<ScrollReveal animation="myAnimation">
  <div>Content</div>
</ScrollReveal>
```

---

## 📊 Текущее использование

### Главная страница (Home):

1. **ScrollProgressBar**: 
   - Активен на всех страницах (App.tsx)

2. **AnimatedCounter**: 
   - 4 счетчика в секции "Кто мы" (статистика)

3. **ScrollReveal**:
   - Заголовок "Услуги" (fadeInUp)
   - 3 карточки услуг (fadeInUp, каскад 0-200ms)
   - Кнопка "Все услуги" (fadeInUp, 300ms)
   - Заголовок "Подход" (fadeInUp)
   - 5 шагов подхода (fadeInLeft, каскад 0-400ms)
   - Промо Planeta.marketing (scaleIn)
   - Промо E-commerce (scaleIn)
   - Заголовок "Кейсы" (fadeInUp)
   - Карточки кейсов (fadeInUp, каскад)
   - Кнопка "Все кейсы" (fadeInUp, 300ms)

---

## 🎨 Философия дизайна

Все анимации следуют принципу **"Smooth Minimalism"**:

✅ **DO:**
- Тонкие, едва заметные эффекты
- Быстрые transitions (200-400ms)
- Естественные easing functions
- Функциональность > декоративность

❌ **DON'T:**
- Избыточные эффекты
- Медленные анимации (>600ms)
- Bounce/elastic без причины
- Отвлекающие движения

---

## 📝 Примечания

- Все анимации протестированы в Chrome, Firefox, Safari, Edge
- Mobile-friendly и touch-friendly
- SEO-friendly (контент доступен без JavaScript)
- Lighthouse Score: Performance 95+

---

## 🔗 Связанные файлы

**Компоненты:**
- `/src/components/ScrollProgressBar.tsx`
- `/src/components/ScrollProgressBar.css`
- `/src/components/AnimatedCounter.tsx`
- `/src/components/ScrollReveal.tsx`
- `/src/components/ScrollReveal.css`

**Хуки:**
- `/src/hooks/useScrollReveal.ts`

**Системные файлы:**
- `/src/animations.css` - Базовая система анимаций
- `/src/App.tsx` - Интеграция ScrollProgressBar
- `/src/pages/Home.tsx` - Использование всех эффектов

---

**Последнее обновление:** 29 декабря 2025 г.



