# Настройка ngrok для доступа к форме генерации договоров

## 🌐 Описание

Используйте ngrok для создания публичного URL к форме генерации договоров, который можно отправлять клиентам.

## 🚀 Быстрый старт

### 1. Запустите приложение локально

```bash
cd /Users/eldar/эльдар.маркетинг
npm run dev
```

Приложение запустится на порту 3000.

### 2. Запустите ngrok

В новом терминале:

```bash
ngrok http 3000 --domain=strattera.ngrok.app
```

Если у вас статический домен `strattera.ngrok.app`, он будет использован.

### 3. Отправьте ссылку клиенту

Ваша ссылка на форму генерации договоров:

```
https://strattera.ngrok.app/contract
```

## 📋 Полный процесс работы

### Шаг 1: Подготовка
```bash
# В первом терминале - запуск приложения
cd /Users/eldar/эльдар.маркетинг
npm run dev

# Дождитесь сообщения:
# ➜  Local:   http://localhost:3000/
```

### Шаг 2: Создание туннеля
```bash
# Во втором терминале - запуск ngrok
ngrok http 3000 --domain=strattera.ngrok.app

# Или без статического домена:
ngrok http 3000
```

### Шаг 3: Получение URL
После запуска ngrok покажет:
```
Session Status                online
Account                       your-account@email.com
Version                       3.x.x
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://strattera.ngrok.app -> http://localhost:3000
```

### Шаг 4: Отправка ссылки клиенту

**Основная ссылка (главная страница):**
```
https://strattera.ngrok.app
```

**Прямая ссылка на форму договора:**
```
https://strattera.ngrok.app/contract
```

## 📧 Шаблон сообщения для клиента

```
Добрый день!

Для заключения договора на услуги маркетингового аудита, 
пожалуйста, заполните форму по ссылке:

https://strattera.ngrok.app/contract

Система автоматически сгенерирует договор с вашими реквизитами.
Стоимость услуг: 15 000 ₽ / час

После заполнения формы договор скачается в формате PDF.

С уважением,
Эльдар Бурханов
eldar@eldar.marketing
+7 (987) 616-58-60
```

## 🔧 Полезные команды ngrok

### Просмотр статистики запросов
Откройте в браузере:
```
http://localhost:4040
```

Здесь вы увидите:
- Все HTTP запросы в реальном времени
- Подробности каждого запроса
- Возможность повторить запрос

### Использование конфигурационного файла

Создайте файл `~/.ngrok2/ngrok.yml`:

```yaml
version: "2"
authtoken: ваш_токен_здесь
tunnels:
  eldar-marketing:
    proto: http
    addr: 3000
    domain: strattera.ngrok.app
```

Затем запускайте:
```bash
ngrok start eldar-marketing
```

## 🎯 Рекомендации

### 1. Держите сервер запущенным
- Запускайте `npm run dev` в фоне
- Используйте `screen` или `tmux` для долгих сессий
- Или используйте PM2 для автоматического перезапуска

### 2. Мониторинг
- Следите за ngrok Web Interface (http://localhost:4040)
- Проверяйте консоль приложения на ошибки

### 3. Безопасность
- Используйте статический домен strattera.ngrok.app
- Не публикуйте ссылку в открытых источниках
- Отправляйте только конкретным клиентам

## ⚡️ Альтернатива: PM2 для автоматического перезапуска

### Установка PM2
```bash
npm install -g pm2
```

### Создание ecosystem файла
Создайте `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'eldar-marketing',
    script: 'npm',
    args: 'run dev',
    cwd: '/Users/eldar/эльдар.маркетинг',
    watch: false,
    env: {
      NODE_ENV: 'development',
    }
  }]
}
```

### Запуск через PM2
```bash
cd /Users/eldar/эльдар.маркетинг
pm2 start ecosystem.config.js

# Просмотр логов
pm2 logs eldar-marketing

# Остановка
pm2 stop eldar-marketing

# Автозапуск при старте системы
pm2 startup
pm2 save
```

## 🌍 Альтернативные способы публикации

### 1. Vercel (рекомендуется для продакшна)
```bash
npm install -g vercel
vercel --prod
```

Получите постоянный URL типа `eldar-marketing.vercel.app`

### 2. Netlify
```bash
npm run build
netlify deploy --prod
```

### 3. GitHub Pages (только статика)
```bash
npm run build
# Настройте gh-pages
```

## 🐛 Устранение проблем

### ngrok не запускается
```bash
# Проверьте, что ngrok установлен
ngrok version

# Авторизуйтесь
ngrok config add-authtoken ваш_токен
```

### Порт 3000 занят
```bash
# Найдите процесс на порту 3000
lsof -i :3000

# Убейте процесс
kill -9 PID

# Или используйте другой порт
npm run dev -- --port 3001
ngrok http 3001
```

### Форма не работает через ngrok
1. Проверьте, что приложение работает локально: http://localhost:3000/contract
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что ngrok правильно пробросил порт

## 📱 Тестирование

### 1. Локальное тестирование
```
http://localhost:3000/contract
```

### 2. Через ngrok
```
https://strattera.ngrok.app/contract
```

### 3. Мобильное тестирование
- Откройте ngrok URL на телефоне
- Проверьте адаптивность формы
- Протестируйте генерацию PDF на мобильном

## 🔒 Безопасность

### Защита доступа (опционально)

Добавьте базовую аутентификацию в ngrok:

```bash
ngrok http 3000 --domain=strattera.ngrok.app --basic-auth="user:password"
```

Затем сообщите клиенту:
- Username: user
- Password: password

## 📊 Мониторинг использования

### ngrok Dashboard
- Войдите на https://dashboard.ngrok.com
- Смотрите статистику использования
- Управляйте доменами и туннелями

### Application Logs
```bash
# В терминале с npm run dev
# Вы увидите все запросы к серверу

# В ngrok Web Interface (http://localhost:4040)
# Подробная информация о каждом HTTP запросе
```

## 🎓 Best Practices

1. **Тестируйте перед отправкой**
   - Заполните форму сами
   - Проверьте корректность PDF
   - Убедитесь, что все поля работают

2. **Информируйте клиента**
   - Объясните, какие данные нужны
   - Укажите примерное время заполнения (5-10 минут)
   - Предупредите о необходимости точности данных

3. **Сохраняйте контроль**
   - Следите за запросами в ngrok Dashboard
   - Проверяйте консоль на ошибки
   - Будьте готовы помочь клиенту

---

**Полезные ссылки:**
- [Документация ngrok](https://ngrok.com/docs)
- [ngrok Dashboard](https://dashboard.ngrok.com)
- [PM2 Documentation](https://pm2.keymetrics.io)

**Версия:** 1.0  
**Дата:** 29 декабря 2025

