# ⚡ Быстрый деплой на Beget

## 🚀 За 5 минут

### 1. Скачайте архив
📦 Файл: **`eldar-marketing-deploy.zip`** (находится в корне проекта)

### 2. Войдите в панель Beget
🔗 https://beget.com/ → Файловый менеджер

### 3. Загрузите файлы
1. Откройте папку `yourdomain.ru/public_html/`
2. Удалите старые файлы (кроме `.htpasswd`, `.well-known`)
3. Загрузите архив `eldar-marketing-deploy.zip`
4. Распакуйте архив
5. Переместите файлы из папки `dist/` в корень `public_html/`

**Должна получиться структура:**
```
public_html/
├── .htaccess
├── index.html
├── logo.svg
├── robots.txt
└── assets/
```

### 4. Проверьте сайт
✅ Откройте `https://yourdomain.ru`

---

## 🔄 Обновление сайта

После изменений в коде:

```bash
# 1. Соберите проект
npm run build

# 2. Создайте новый архив
zip -r deploy-new.zip dist/

# 3. Загрузите на Beget и замените файлы
```

---

## ❓ Проблемы?

**404 при обновлении страницы** → Проверьте `.htaccess`  
**Белый экран** → Проверьте консоль браузера (F12)  
**Нет стилей** → Очистите кэш (Ctrl+Shift+Del)

📖 Подробная инструкция: **`DEPLOY_BEGET.md`**

---

**Время**: ~5 минут  
**Архив**: `eldar-marketing-deploy.zip` (готов к загрузке)

